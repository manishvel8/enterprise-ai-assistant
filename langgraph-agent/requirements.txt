# LangGraph + LangChain
langgraph>=0.2.28
langchain-core>=0.2.38
langchain-openai>=0.1.25

# Langfuse — LLM observability and tracing
# After installing: docker compose -f docker-compose.langfuse.yml up -d
# Then open http://localhost:3000 → create project → copy keys into .env
langfuse>=2.0.0,<3

# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Vector DB
qdrant-client>=1.12.0

# Web Search
tavily-python>=0.3.9

# Model Context Protocol (MCP server + client)
mcp>=1.9.0,<3

# Pydantic & config
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# Utilities
httpx>=0.27.0
aiofiles>=23.2.1
tiktoken>=0.7.0

# PostgreSQL — conversation history + memory
# Uses the same Postgres instance as enterprise-ai-assistant (port 5433)
psycopg2-binary>=2.9.9

# pgvector — semantic similarity search over message embeddings
# Run: CREATE EXTENSION IF NOT EXISTS vector; (in schema.sql)
pgvector>=0.2.5
